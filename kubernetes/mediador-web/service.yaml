apiVersion: v1
kind: Service
metadata:
  name: mediador-web-service
  namespace: medisupply
spec:
  type: ClusterIP
  selector:
    app: mediador-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5003
